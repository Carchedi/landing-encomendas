document.addEventListener("DOMContentLoaded", () => {
  // O JavaScript do acordeão vertical foi removido,
  // pois o componente foi substituído por abas (tabs) do Bootstrap,
  // que funcionam nativamente sem JS customizado.
});
